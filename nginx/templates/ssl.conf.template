# HTTPS：反代到同 compose 内的 web 服务
# 证书路径：ssl/xtai-nav.cn.pem、ssl/xtai-nav.cn.key（见 docker-compose.standalone.yml）

server {
    listen 443 ssl;
    http2 on;
    server_name ${NGINX_DOMAIN} www.${NGINX_DOMAIN};

    ssl_certificate     /etc/nginx/ssl/xtai-nav.cn.pem;
    ssl_certificate_key /etc/nginx/ssl/xtai-nav.cn.key;

    ssl_session_timeout 1d;
    ssl_session_cache shared:SSL:10m;
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        proxy_pass http://web:${NGINX_APP_PORT};
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
